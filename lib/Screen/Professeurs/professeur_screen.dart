import 'package:flutter/material.dart';

class HomeProfesseurScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Accueil Professeur")),
      body: Center(child: Text("Bienvenue, Professeur !")),
    );
  }
}
